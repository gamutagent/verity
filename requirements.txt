aiohttp>=3.9,<4.0
pyyaml>=6.0,<7.0

# Cloud provider backends — install only what you need:
# pip install intel-sweep[gcp]     # Google Cloud
# pip install intel-sweep[aws]     # AWS
# pip install intel-sweep[azure]   # Azure

# GCP (Secret Manager + Firestore)
# google-cloud-secret-manager>=2.20
# google-cloud-firestore>=2.16

# AWS (Secrets Manager)
# boto3>=1.34

# Azure (Key Vault)
# azure-identity>=1.16
# azure-keyvault-secrets>=4.8

# Dev/test
# pytest>=8.0
# pytest-asyncio>=0.23
